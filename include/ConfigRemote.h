#pragma once

#include "ConfigLocal.h"
#include "Rpc.h"

namespace random_tools {
namespace config {

class ConfigRemote {}; // TODO, implement selfcontained remote config with its own rpc, thread, io service and mutex for config.

}
}
